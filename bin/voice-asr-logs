#!/usr/bin/env bash
set -euo pipefail

SERVER_ROOT="${CODEX_SERVER_ROOT:-${HOME}/.codex/servers}"
VOICE_ASR_HOME="${VOICE_ASR_HOME:-${SERVER_ROOT}/voice-asr}"
cd "$VOICE_ASR_HOME"
docker compose logs -f --tail="${VOICE_ASR_LOG_TAIL:-120}" voice-asr
